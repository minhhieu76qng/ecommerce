import React from 'react';
import Homepage from '../components/homepage/Homepage';

const Home = () => {
  return (
    <React.Fragment>
      <Homepage />
    </React.Fragment>
  );
};

export default Home;
