import React from 'react';
import HomepageContainer from '../containers/HomepageContainer';

const Home = () => {
  return (
    <React.Fragment>
      <HomepageContainer />
    </React.Fragment>
  );
};

export default Home;
